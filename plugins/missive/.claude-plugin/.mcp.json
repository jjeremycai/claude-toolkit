{
  "mcpServers": {
    "missive": {
      "command": "npx",
      "args": ["tsx", "${CLAUDE_PLUGIN_ROOT}/mcp/server.ts"],
      "env": {
        "MISSIVE_API_TOKEN": "${MISSIVE_API_TOKEN}"
      }
    }
  }
}
